type Query {
    bookById(id: ID): Book
    books: [Book]
}

type Mutation {
    addAuthort(firstName: String, lastName: String):Author
}

type Book {
    id: ID
    name: String
    pageCount: Int
    author: Author
}

type Author {
    id: ID
    firstName: String
    lastName: String
}